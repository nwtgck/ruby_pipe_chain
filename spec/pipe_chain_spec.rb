require 'spec_helper'

describe PipeChain do
  it 'has a version number' do
    expect(PipeChain::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
